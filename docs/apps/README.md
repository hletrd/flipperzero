# Apps

| Doc | What |
|---|---|
| [api-compatibility.md](api-compatibility.md) | Why `.fap` files don't always work between firmware variants |

For the curated, tested, working set of apps for **Momentum**: see `forks/momentum-apps/`. The 266 `.fap` files in `/apps/` on the deployed SD card were extracted from a Momentum build's `resources.tar.gz` and are all known-good for the currently-flashed firmware.
